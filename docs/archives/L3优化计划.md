

# EnlightenLM L3 贝叶斯元控制层升级计划

## 0. 项目现状与目标

| 维度 | 当前状态 | 目标状态 |
|------|---------|---------|
| L3 控制器 | ✅ 贝叶斯病因推断器已实现 | 优化超参数，提升性能 |
| 数据采集 | ✅ 基础 Trace 采集器已实现 | 完整的三类条件数据 |
| 可视化分析 | ✅ 基础可视化脚本已实现 | 可区分性验证与分析 |
| 实验验证 | ✅ 测试用例通过 | 完整的性能对比实验 |

**核心目标：** 实现贝叶斯 L3 控制器，验证信号可区分性，提升系统鲁棒性。

---

## 1. 实施计划

### 1.1 第一阶段：贝叶斯 L3 控制器实现

**时间估计：** 2-3 天

**具体任务：**
1. **实现 `BayesianL3Controller` 类**
   - 文件：`enlighten/l3_controller.py`
   - 核心功能：贝叶斯病因推断、动态温度调节、连续截断信心
   - 实现要点：
     - 贝叶斯更新算法（解析计算后验）
     - 三种假设的生成模型
     - 温度调节策略
     - 连续截断信心计算

2. **集成到现有系统**
   - 修改 `hybrid_architecture.py`，支持切换贝叶斯 L3 控制器
   - 添加配置选项 `use_bayesian_l3`

3. **单元测试**
   - 编写 `test_bayesian_l3.py`
   - 测试贝叶斯更新、温度调节、截断信心计算

**成功标准：**
- `BayesianL3Controller` 类实现完成
- 集成测试通过
- 单元测试覆盖率 >80%

### 1.2 第二阶段：Trace 数据采集与可区分性验证

**时间估计：** 3-4 天

**具体任务：**
1. **运行 Trace 数据采集**
   - 使用 `tests/l3_trace_collector.py`
   - 三种条件各运行 50 次会话
   - 保存 CSV 数据文件

2. **数据可视化与分析**
   - 使用 `tests/l3_visualization.py`
   - 生成 3D 散点图、时序轨迹图、分类准确率曲线
   - 分析噪声与偏见的区分效果

3. **可区分性验证**
   - 计算窗口长度 L=10 时的分类准确率
   - 验证是否达到 >85% 的准确率
   - 执行 t 检验，验证噪声与偏见的显著差异

**成功标准：**
- 采集至少 150 条有效记录（每种条件 50 条）
- L=10 时分类准确率 >85%
- 噪声与偏见在 `σ_H²` 和 `k_H` 维度上有显著差异（p<0.01）

### 1.3 第三阶段：性能对比与优化

**时间估计：** 2-3 天

**具体任务：**
1. **性能对比实验**
   - 在偏见/噪声混合测试集上比较新旧 L3 控制器
   - 评估指标：任务完成准确率、截断误报率、响应时间

2. **超参数优化**
   - 基于 trace 数据拟合贝叶斯 SSM 的超参数
   - 调整温度调节策略参数

3. **文档与论文准备**
   - 更新 API 文档
   - 准备论文所需的实验数据和图表

**成功标准：**
- 贝叶斯 L3 控制器在所有指标上优于硬编码版本
- 超参数优化完成
- 论文所需数据和图表准备就绪

---

## 2. 技术实现细节

### 2.1 贝叶斯 L3 控制器核心算法

```python
class BayesianL3Controller:
    def __init__(self, prior_probs=[0.8, 0.1, 0.1]):
        self.p_H = np.array(prior_probs)   # P(H1), P(H2), P(H3)
        # 三种假设的似然模型参数
        self.models = {
            'normal': {
                'mu_std': 0.01,  # 过程噪声
                'obs_std': 0.02   # 观测噪声
            },
            'noise_injection': {
                'mu_std': 0.5,    # 高过程噪声
                'obs_std': 0.1
            },
            'bias_injection': {
                'mu_std': 0.01,
                'obs_std': 0.02,
                'step_gain': 2.0  # 偏见阶跃增益
            }
        }

    def update(self, o_int, external_feedback=None):
        """
        o_int: (μ_H, σ_H², k_H, p_harm_raw)
        """
        # 计算似然
        likelihoods = []
        for condition in ['normal', 'noise_injection', 'bias_injection']:
            model = self.models[condition]
            # 简化的似然计算
            if condition == 'normal':
                # 正常状态：低方差，稳定趋势
                lik = np.exp(-0.5 * (o_int['sigma_H2']/0.05)**2) * \
                      np.exp(-0.5 * (abs(o_int['k_H'])/0.1)**2)
            elif condition == 'noise_injection':
                # 噪声状态：高方差
                lik = np.exp(-0.5 * ((o_int['sigma_H2']-0.2)/0.1)**2)
            else:  # bias_injection
                # 偏见状态：低均值，高危险值
                lik = np.exp(-0.5 * ((o_int['mu_H']-0.3)/0.1)**2) * \
                      np.exp(-0.5 * ((o_int['p_harm_raw']-0.5)/0.2)**2)
            likelihoods.append(lik)

        # 贝叶斯更新
        unnorm = self.p_H * np.array(likelihoods)
        self.p_H = unnorm / unnorm.sum()

        # 温度调节
        tau_default = 1.0
        tau = tau_default * (1.0 - 0.5 * self.p_H[2]) * (1.0 + 0.3 * self.p_H[1])
        tau = np.clip(tau, 0.2, 2.0)

        # 连续截断信心
        p_harm = self.p_H[2] * 0.8 + o_int['p_harm_raw'] * 0.2

        return {
            'tau': tau,
            'p_harm': p_harm,
            'posterior': self.p_H,
            'condition': ['normal', 'noise_injection', 'bias_injection'][np.argmax(self.p_H)]
        }
```

### 2.2 数据采集与分析流程

1. **数据采集**：
   - 运行 `python tests/l3_trace_collector.py`
   - 生成 `results/l3_trace_*.csv` 文件

2. **数据可视化**：
   - 3D 散点图：`python tests/l3_visualization.py --mode scatter3d --data results/l3_trace_*.csv`
   - 时序轨迹：`python tests/l3_visualization.py --mode trajectory --data results/l3_trace_*.csv`
   - 分类评估：`python tests/l3_visualization.py --mode classify --data results/l3_trace_*.csv`

3. **可区分性分析**：
   - 分析 3D 散点图中的聚类情况
   - 检查分类准确率曲线
   - 验证噪声与偏见的统计差异

---

## 3. 预期成果

1. **技术成果**：
   - 实现贝叶斯 L3 控制器，替换硬编码阈值
   - 验证三种条件下信号的可区分性
   - 提升系统对噪声和偏见的鲁棒性

2. **实验数据**：
   - 完整的三类条件 Trace 数据
   - 可区分性验证结果
   - 新旧 L3 控制器性能对比

3. **论文支持**：
   - 可区分性定理的经验证实
   - 贝叶斯病因推断框架的工程实现
   - 实验数据和分析结果

---

## 4. 风险与应对策略

| 风险 | 可能性 | 影响 | 应对策略 |
|------|--------|------|----------|
| 数据采集失败 | 中 | 延迟项目进度 | 提前测试采集脚本，准备备用 API 密钥 |
| 可区分性验证不通过 | 低 | 需调整模型参数 | 增加数据量，优化特征提取，调整分类算法 |
| 性能对比不达标 | 低 | 需优化贝叶斯算法 | 调整超参数，优化温度调节策略，增加训练数据 |

---

## 5. 执行时间表

| 阶段 | 任务 | 时间 | 状态 | 负责人 |
|------|------|------|------|--------|
| 第一阶段 | 贝叶斯 L3 控制器实现 | 2026-04-24 | ✅ 已完成 | 开发者 |
| 第二阶段 | Trace 数据采集与分析 | 2026-04-25 至 2026-04-28 | 进行中 | 开发者 |
| 第三阶段 | 性能对比与优化 | 2026-04-29 至 2026-05-01 | 计划中 | 开发者 |
| 文档与论文 | 整理实验数据与文档 | 2026-05-02 至 2026-05-04 | 计划中 | 开发者 |

**总计：** 11 天

---

## 6. 结论

本计划通过实现贝叶斯 L3 控制器，验证信号可区分性，提升系统鲁棒性，为 EnlightenLM 项目的进一步发展奠定基础。同时，实验数据和分析结果将为论文提供有力支持，实现理论与实践的结合。

将这个md文件放入 `docs/` 或项目根目录，即可作为升级蓝图开始实施。第二步是相图验证，第三步是对比评估。需要进一步细化某一步的代码或实验脚本，可以随时继续。